document.addEventListener('DOMContentLoaded',()=>{
  const track=document.querySelector('#routine .routine-steps');const cards=track?[...track.querySelectorAll('.routine-step')]:[];if(!track||cards.length<2)return;
  const images=['assets/routine-01-drop.png','assets/routine-02-pour.png','assets/routine-03-steep.png'],stages=['DROP','POUR','STEEP & SIP'];
  cards.forEach((card,i)=>{const image=card.querySelector('.routine-image');if(image){image.style.backgroundImage=`url('${images[i]}')`;image.style.backgroundSize='cover';image.style.backgroundPosition='center'}});
  const controls=document.createElement('div');controls.className='routine-interactive';controls.innerHTML='<div class="routine-progress" aria-label="Brewing progress"></div><div class="routine-controls"><button type="button" class="routine-control routine-prev">← Previous</button><button type="button" class="routine-control routine-next">Next →</button></div><p class="routine-swipe-hint">Swipe to move through the three steps</p>';track.after(controls);
  const progress=controls.querySelector('.routine-progress');stages.forEach((stage,i)=>{const b=document.createElement('button');b.type='button';b.className='routine-progress-dot';b.textContent=stage;b.dataset.index=i;progress.appendChild(b)});
  let current=0;const mobile=()=>matchMedia('(max-width:800px)').matches;function show(next,behavior='smooth'){current=Math.max(0,Math.min(cards.length-1,next));if(mobile())cards[current].scrollIntoView({behavior,block:'nearest',inline:'center'});progress.querySelectorAll('button').forEach((b,i)=>b.classList.toggle('is-active',i===current));controls.querySelector('.routine-prev').disabled=current===0;controls.querySelector('.routine-next').disabled=current===cards.length-1}
  controls.querySelector('.routine-prev').addEventListener('click',()=>show(current-1));controls.querySelector('.routine-next').addEventListener('click',()=>show(current+1));progress.querySelectorAll('button').forEach(b=>b.addEventListener('click',()=>show(Number(b.dataset.index))));
  if('IntersectionObserver' in window){const observer=new IntersectionObserver(entries=>{if(!mobile())return;const visible=entries.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(visible){current=cards.indexOf(visible.target);show(current,'auto')}},{root:track,threshold:[.55,.75]});cards.forEach(card=>observer.observe(card))}
  addEventListener('resize',()=>show(Math.min(current,cards.length-1),'auto'));show(0,'auto');
});