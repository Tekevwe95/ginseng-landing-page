const packageButtons=document.querySelectorAll(".package");const packageSelect=document.getElementById("package");packageButtons.forEach(button=>button.addEventListener("click",()=>{packageSelect.value=button.dataset.package;document.getElementById("order").scrollIntoView({behavior:"smooth"})}));
const form=document.getElementById("orderForm");const confirmation=document.getElementById("confirmation");const message=document.getElementById("formMessage");const summary=document.getElementById("orderSummary");
form.addEventListener("submit",event=>{event.preventDefault();const data=new FormData(form);const orderNumber="POD-"+Date.now().toString().slice(-6);summary.innerHTML=`<div class="summary"><strong>Order ${orderNumber}</strong><span>${data.get("package")}</span><span>Customer: ${data.get("name")}</span><span>Phone: ${data.get("phone")}</span><span>Delivery: ${data.get("city")}, ${data.get("state")}</span><span class="payment-line">Payment: <strong>PAY ON DELIVERY</strong></span></div>`;message.textContent="";confirmation.classList.remove("hidden");confirmation.scrollIntoView({behavior:"smooth"})});
